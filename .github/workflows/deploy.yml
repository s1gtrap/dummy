on: push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Install and Build 🔧
        run: |
          npm ci
          npm run build

    - uses: JamesIves/github-pages-deploy-action@v4.3.3
      with:
        branch: gh-pages
        folder: build
